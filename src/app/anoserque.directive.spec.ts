import { AnoserqueDirective } from './anoserque.directive';

describe('AnoserqueDirective', () => {
  it('should create an instance', () => {
    const directive = new AnoserqueDirective();
    expect(directive).toBeTruthy();
  });
});
